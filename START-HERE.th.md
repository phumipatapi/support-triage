# เริ่มใช้งาน Support Ticket Triage

ระบบนี้ช่วยเจ้าหน้าที่อ่านเคสลูกค้า จัดความเร่งด่วน ค้น FAQ และแนะนำขั้นตอนถัดไป เก็บประวัติให้คุยต่อได้ เป็น HTTP API พร้อม terminal chat ไม่มีหน้าเว็บ FAQ เป็นข้อมูลสมมติ และเครื่องมือสร้าง incident จำลองเขียนข้อมูลจริงลง SQLite แต่ไม่ได้แจ้งทีม คืนเงิน หรือแก้บัญชีลูกค้า

## เปิดระบบ

ต้องมี Node.js 22.13 ขึ้นไปและ npm เปิด terminal ที่โฟลเดอร์โปรเจกต์แล้วรัน:

```sh
npm run setup
```

คำสั่งนี้ติดตั้ง dependencies เตรียม `.env.local` แล้วเปิด API ที่ `http://127.0.0.1:3000` ครั้งถัดไปใช้ `npm run dev` ได้เลย

setup จะรักษาค่าเดิมถ้ามี `.env.local` อยู่แล้ว ถ้าเป็นสำเนาใหม่จะเลือก OpenAI เมื่อมีคีย์ใน environment ไม่เช่นนั้นจะใช้ mock แบบ offline หากต้องการบังคับโหมด offline ให้ใช้ `npm run setup:offline` แทน สำหรับเครื่องที่ตั้งค่า Jev + OpenAI ไว้ ใช้ `npm run dev` เพื่อเปิดตามค่าเดิม

## พิมพ์ข้อความเอง

ปล่อย terminal เซิร์ฟเวอร์ไว้ เปิดอีก terminal ที่โฟลเดอร์เดียวกัน แล้วรัน:

```sh
npm run chat
```

พิมพ์ทีละข้อความและรอคำตอบ:

```text
My Mac is in dark mode, but the app stays light after selecting System Default.
Also, can I schedule dark mode to turn on at 6pm?
/operator
สรุปว่าลูกค้ามีปัญหาอะไร และระบบได้ทำอะไรไปจริงแล้วบ้าง
```

สองข้อความแรกเป็นเคสเดียวกัน ระบบควรยังจำบั๊กเดิมได้และแนะนำส่งให้ engineering ส่วนคำถามภาษาไทยของเจ้าหน้าที่ควรได้สรุปภาษาไทย ข้อมูลภาษากับความรู้สึกของลูกค้ายังอิงข้อความลูกค้า

| สิ่งที่แสดง | ความหมาย |
| --- | --- |
| ผู้ช่วย | `reply` ข้อความที่โมเดลเรียบเรียงจากหลักฐาน |
| ความเร่งด่วน | `low`, `medium`, `high` หรือ `critical` |
| ขั้นตอนถัดไป | ตอบจาก FAQ, แนะนำส่งผู้เชี่ยวชาญ หรือให้คนตรวจ |
| Incident | ผลจากเครื่องมือ incident จำลอง หรือไม่มี |

ค่าที่แสดงคือผลของโมเดลและกฎระบบ ไม่ใช่การรับรองว่าคำตอบถูกทุกข้อ โดยเฉพาะข้อความที่อ้างว่าทำอะไรไปแล้วต้องตรงกับผลเครื่องมือจริง

ถ้าระบบตัดสินให้คนตรวจหรือส่งผู้เชี่ยวชาญแล้ว แต่ร่างคำตอบผิดภาษา หรืออ้างการดำเนินการที่ไม่ได้ทำ ระบบอาจใช้ข้อความแนะนำส่งต่อที่กำหนดไว้แทน โดย API ระบุ `reply_source: policy_fallback` ไม่ได้เรียกโมเดลเพิ่มและไม่อ้าง FAQ ข้อความนี้บอกให้คนตรวจต่อ ไม่ใช่คำตอบโดยละเอียดที่ผ่านการตรวจแล้ว ความผิดพลาดของ provider หรือข้อมูลตอบกลับที่ผิดรูปแบบยังต้องใช้ `/retry`

คำสั่งที่ใช้บ่อย:

- `/new` เริ่มเคสใหม่ ใช้แยกเรื่อง billing, outage และธีม ไม่ให้ปัญหาปนกัน
- `/operator` ถามในฐานะเจ้าหน้าที่; `/customer` กลับไปเพิ่มข้อความลูกค้า
- `/history` อ่านประวัติเคสและ audit
- `/status` ดูหมายเลขเคสปัจจุบัน
- `/retry` ลองคำขอที่ยังไม่ทราบผลซ้ำด้วย body และ key เดิม
- `/exit` ออก

เห็น `ลูกค้า>` หรือ `เจ้าหน้าที่>` ให้พิมพ์ข้อความคุยได้ หากเห็น `PS ...>` แปลว่าอยู่ใน PowerShell ต้องรัน `npm run chat` ก่อน ประวัติอยู่ที่เซิร์ฟเวอร์ และตัว client บันทึกเคสปัจจุบันกับคำขอที่ค้างไว้ให้เปิดกลับมาคุยต่อได้

## ใช้ OpenAI จริง

ผู้ตรวจใช้ OpenAI key อย่างเดียวได้ เปิด PowerShell ที่โฟลเดอร์โปรเจกต์:

```powershell
$env:OPENAI_API_KEY = 'ใส่คีย์ของคุณ'
npm run setup:openai
```

หรือแก้ `.env.local` แล้วรัน `npm run dev`:

```dotenv
DECISION_PROVIDER=openai
REPLY_PROVIDER=openai
OPENAI_API_KEY=ใส่คีย์ของคุณ
OPENAI_MODEL=gpt-4.1-mini
```

ถ้าต้องการ Jev วิเคราะห์และ GPT เขียนคำตอบตามโหมด hybrid:

```dotenv
DECISION_PROVIDER=jev
REPLY_PROVIDER=openai
TYPESAFE_API_KEY=ใส่คีย์ Jev
OPENAI_API_KEY=ใส่คีย์ OpenAI
```

หยุดเซิร์ฟเวอร์ด้วย Ctrl+C แล้วรัน `npm run dev` ใหม่หลังเปลี่ยนค่า อย่าส่งคีย์ผ่านแชตหรือ commit `.env.local` โหมดจริงมีค่า API ตามผู้ให้บริการ คำสั่ง chat ไม่ได้เปลี่ยน provider เอง

## ทดสอบและอ่านผล

```sh
npm run check
npm run smoke
npm run eval
```

สามคำสั่งนี้ตรวจโค้ด การ retry หลัง restart และ eval แบบ offline โดยไม่เรียกโมเดลจริง ส่วน `npm run eval:openai` ทดลองด้วย OpenAI อย่างเดียว และ `npm run eval:jev` ทดลอง Jev + OpenAI ทั้งสองใช้คีย์จริง มีค่า API และเขียนรายงานไว้ใต้ `evals/reports/` โดยไม่เปลี่ยน `.env.local`

`npm run demo -- theme-bug-thread` ใช้บทสนทนาที่เตรียมไว้และทดสอบส่งซ้ำอัตโนมัติ แต่ `npm run chat` เหมาะกับข้อความที่พิมพ์เอง

อ่านขั้นตอนสำหรับผู้ตรวจและ API ที่ [README.md](README.md), ผลตรวจที่ [VALIDATION.md](VALIDATION.md) และเหตุผลการออกแบบสำหรับส่งงานที่ [WRITEUP.md](WRITEUP.md) หรือ [คำแปลภาษาไทย](docs/WRITEUP.th.md)
